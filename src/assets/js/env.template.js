(function(window) {
  window.env = window.env || {};

  // Variables de entorno
  window.env['HOST_BACKEND'] = '${HOST_BACKEND}';
  window.env['HOST_BACKEND_API'] = '${HOST_BACKEND_API}';
  window.env['HOST_FRONT'] = '${HOST_FRONT}';
})(this);
